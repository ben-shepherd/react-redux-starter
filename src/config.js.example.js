import appAction from "./_actions/app.action";

const config = {
    appVersion: "1.0.0",
    appName: "shift-react-boilerplate",
    apiBaseUrl: "http://api.local/",
    enviroment: appAction.ENV_PRODUCTION,
}

export default config